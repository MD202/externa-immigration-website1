export default function ContactStep({ data, setData }) {
  const field = (key, value) => setData((current) => ({ ...current, [key]: value }));
  return <div><h2 className="font-heading text-4xl leading-tight text-[#0F2433] sm:text-5xl">How can we reach you?</h2><p className="mt-4 text-[#0F2433]/60">Share enough for a confidential initial review. We’ll contact you about next steps.</p><div className="mt-9 grid gap-5 sm:grid-cols-2"><label className="intake-label">Full name<input required value={data.full_name} onChange={(e) => field("full_name", e.target.value)} className="intake-input" /></label><label className="intake-label">Email<input required type="email" value={data.email} onChange={(e) => field("email", e.target.value)} className="intake-input" /></label><label className="intake-label sm:col-span-2">Phone <span className="font-normal opacity-50">(optional)</span><input value={data.phone} onChange={(e) => field("phone", e.target.value)} className="intake-input" /></label><label className="intake-label sm:col-span-2">Brief summary <span className="font-normal opacity-50">(optional)</span><textarea rows="4" value={data.summary} onChange={(e) => field("summary", e.target.value)} className="intake-input resize-none" /></label></div></div>;
}